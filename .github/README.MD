# GitHub Action - Deploy CloudFront Distribution

This is a temporary action, not got general use.

## Example usage

```yaml
uses: eonxcom/eonx-github-actions-cloudfront-deploy
with:
  cmd: jq -n env

```
